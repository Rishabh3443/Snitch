import {Router} from "express"

const router = Router()

/* 
1. authenticate middleware to check user role ..
2. checking the role and is to be a seller to add products only -- authorization middleware.
3. using multer to accepting formdata from frontend
4. parsing complex text like object , array in json format using json.parse.
5. using productsvalidation middleware to validate the frontend incoming data.
6. using controller store the product data in database.
 */

router.post()


export default router;